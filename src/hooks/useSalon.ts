import { useSalon } from "@/components/providers/SalonProvider";

/**
 * Hook to access the current salon configuration.
 * This is just a wrapper around the useSalon context hook for cleaner imports.
 */
export { useSalon };
